<dodgerblue># Security</dodgerblue>

<khaki>$ deno examples/read_file.ts</khaki>

  <gold>⚠️ Deno requests read access to</gold>
  <gold>  "~/.ssh/id_ed25519".</gold>

    Grant? [a/y/n/d]
      a = allow always
      y = allow once
      n = deny once
      d = deny always